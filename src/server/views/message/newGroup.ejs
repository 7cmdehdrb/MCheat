<!DOCTYPE html>
<html lang="en">
    <head>
        <% include ../partial/head %>
    </head>
    <body>
        <% include ../partial/header %>

        <div class="container w-full lg:w-2/3 mx-auto my-5">
            <h1 class="text-center text-4xl my-5">새 그룹채팅</h1>

            <!-- Form -->

            <form method="POST">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                <div class="form-row">
                    <div class="form-group col-md-6">
                        <label for="inputGroup">그룹이름</label>
                        <input type="text" class="form-control" name="name" id="inputGroup" required maxlength="30" placeholder="그룹명(최대 30자)" value="<%= session.user.nickname %>의 그룹채팅" />
                    </div>
                    <div class="form-group col-md-6">
                        <label for="inputPassword">비밀번호</label>
                        <input type="password" class="form-control" name="password" id="inputPassword" required placeholder="그룹 비밀번호" autocomplete="new-password" />
                    </div>
                </div>
                <div class="form-group">
                    <label for="inputIntroduce">그룹소개</label>
                    <input type="text" class="form-control" name="introduce" id="inputIntroduce" placeholder="그룹소개 (최대 300자)" required />
                </div>
                <div class="form-group flex justify-end">
                    <button type="submit" class="btn btn-primary">개설하기</button>
                </div>
            </form>

            <!-- Form End -->
        </div>

        <% include ../partial/foot %>
        <!--  -->
        <% include ../partial/footer %>
    </body>
</html>
