<!DOCTYPE html>
<html lang="en">
    <head>
        <% include ../partial/head %>
    </head>
    <body>
        <% include ../partial/header %>

        <div class="container w-1/2 mx-auto my-10">
            <form method="POST">
                <div class="form-group row">
                    <label for="staticEmail" class="col-sm-2 col-form-label">사용자</label>
                    <div class="col-sm-10">
                        <input type="text" readonly class="form-control-plaintext" id="staticEmail" value="<%= session.user.email %>" />
                    </div>
                </div>
                <div class="form-group row">
                    <label for="inputPassword" class="col-sm-2 col-form-label">관리자키</label>
                    <div class="col-sm-10">
                        <input type="password" name="adminKey" class="form-control" id="inputPassword" placeholder="관리자키를 이용하여 관리자 권한을 부여받으세요" required />
                    </div>
                </div>
                <div class="form-group row">
                    <button class="btn btn-primary mx-auto">관리자권한 받기</button>
                </div>
            </form>
        </div>

        <% include ../partial/foot %> <% include ../partial/footer %>
    </body>
</html>
